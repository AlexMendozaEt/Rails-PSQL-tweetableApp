<!DOCTYPE html>
<html>
  <head>
    <title>Tweetable</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header  class="header_box">
      <div>
        <%= link_to home_path do %>
          <div>
            <%= image_tag 'Icon-tweetable.png' %>
          </div>
        <% end %>
      </div>
      <div>
        <input type="checkbox" id="check" />
        <label for="check" class="hamburger-menu" id="close">
          <%= image_tag 'Icon-hamburger-menu.svg' %>
        </label>
        <label for="check" class="hamburger-menu" id="open">
          <%= image_tag 'Icon-close.svg' %>
        </label>
        <div class="nav-links">
          <% if user_signed_in? %>
          <ul class="style-none">
            <li><%= link_to "Profile", user_path(current_user.id)%></li>
            <li><%= button_to "Log Out", destroy_user_session_path, method: :delete, data: { confirm: "Are you sure you want to log out?" },class: "buttom_logout" %></li>
          </ul>
          <% else %>
          <ul class="style-none">
            <li><span> Login to start the tweeting madness! <span></li>
            <li class="buttom_login"><%= link_to "Login", user_session_path %></li>
          </ul>
          <% end %>
        <div>
      </div>
    </header>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
    <footer class="footer_box">
      <div>
          <p class="text-center">© 2023 - Codeable Tweetable</p>
          <p class="text-center">Build with ♥ by</p>
          <div>
            <%= link_to "https://www.linkedin.com/in/alex-mendoza-etchebarne/" do %>
              <div class="footer_link">
                <div class="icon_footer">
                  <%= image_tag 'Icon-linkedin.png' %>
                </div>
                <p> Alex Mendoza</p>
              </div>
            <% end %>
          </div>
      </div>
    </footer>
  </body>
</html>
