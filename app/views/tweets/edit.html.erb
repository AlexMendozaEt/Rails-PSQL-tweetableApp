<h1>Edit Tweet</h1>

<%= form_with(model: @tweet) do |form| %>
  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit :Update %>
  </div>
<% end %>
